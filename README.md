# OB MiFi Manager

> **Disclaimer**: This project was entirely generated by AI (Claude) for entertainment and experimental purposes only. It is not an officially supported product. Use at your own risk.

An Android management app for LTE portable WiFi hotspots, replacing the default web-based management interface at `192.168.1.1` with a native Material Design 3 experience.

## Supported Devices

- LBF1A series LTE portable WiFi routers (and other devices running the same firmware)
- Default management address: `192.168.1.1`

## Features

### Dashboard
- Real-time signal strength, battery, and network speed (1-second refresh)
- Data plan usage progress bar (tap to view details)
- Cellular network toggle
- Session / total traffic statistics
- Live uptime counter

### Signal Monitor
- RSRP / SINR / RSRQ gauge charts
- Cell info: MCC, MNC, TAC, PCI, eNB ID, Cell ID
- Band, bandwidth, EARFCN, CQI, RSSI, TX power

### WiFi Settings
- View / change WiFi name and password
- Security mode switching (WPA2, WPA3, Mixed)
- Advanced: channel, bandwidth, max clients, AP isolation

### Device Management
- Online device list with real-time refresh
- Block / unblock devices
- Device info: model, firmware, IMEI, ICCID
- SIM card management and switching (eSIM1 / eSIM2 / auto-select)
- Network mode switching (2G / 3G / 4G / Auto)
- DHCP settings viewer
- Change admin password / restart device / factory reset

### Data Plan Query
- Query remaining data, expiry date, and balance
- Recharge number is user-inputted, supports multiple users

### Internet Authentication
- SMS verification for device internet access

## Login

- Enter the router admin password to log in
- "Remember password" option saves credentials for auto-login
- Logging out does not clear saved password (when remember is checked)

## Tech Stack

- **Language**: Kotlin
- **UI**: Jetpack Compose + Material Design 3
- **Architecture**: MVVM + Hilt DI
- **Networking**: Retrofit2 + OkHttp + HTTP Digest Auth
- **Min SDK**: Android 8.0 (API 26)

## Build

The project uses GitHub Actions for CI/CD. Push code or create a tag to trigger a build:

```bash
# Create a release
git tag v1.0.0
git push origin v1.0.0
```

Local build:

```bash
./gradlew assembleDebug    # Debug build
./gradlew assembleRelease  # Release build
```

## Download

Go to the [Releases](https://github.com/Flymero/OB-MiFiManager/releases) page to download the latest APK.

## Permissions

| Permission | Purpose |
|------------|---------|
| `INTERNET` | Access router API and plan query service |
| `ACCESS_NETWORK_STATE` | Check network connectivity |
| `ACCESS_WIFI_STATE` | Check WiFi connection status |

## License

MIT License
